extends layout


block content

  if users
    table
      tr
        th Usuario
        th PokeID
        th Team
        th Nombre y Apellido
      each user in users
        tr
          td= user.username
          td= user.pokeid          
          td= user.team
          td= user.nomape
          
  else
    p No registrations yet :(