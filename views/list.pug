extends layout


block content

  if users.length
    table
      tr
        th Name
        th Email
      each user in users
        tr
          td= user.pokeid
          td= user.email
  else
    p No registrations yet :(